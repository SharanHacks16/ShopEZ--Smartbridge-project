import Transaction from '../models/Transaction.js';
export const getTransactions = async (req, res, next) => { try { const { search = '', type, status, page = 1, limit = 10, from, to } = req.query; const query = req.user.role === 'admin' ? {} : { user: req.user._id }; if (type) query.buyOrSell = type; if (status) query.status = status; if (from || to) query.timestamp = { ...(from && { $gte: new Date(from) }), ...(to && { $lte: new Date(to) }) }; const skip = (Number(page) - 1) * Number(limit); let transactions = await Transaction.find(query).populate('stock').populate('user', 'name email').sort({ timestamp: -1 }).skip(skip).limit(Number(limit)); if (search) transactions = transactions.filter((tx) => tx.stock?.symbol?.toLowerCase().includes(search.toLowerCase())); const total = await Transaction.countDocuments(query); res.json({ success: true, data: { transactions, total, page: Number(page), pages: Math.ceil(total / Number(limit)) } }); } catch (error) { next(error); } };
export const updateTransactionStatus = async (req, res, next) => { try { const transaction = await Transaction.findByIdAndUpdate(req.params.id, { status: req.body.status }, { new: true, runValidators: true }); if (!transaction) { res.status(404); throw new Error('Transaction not found'); } res.json({ success: true, data: transaction }); } catch (error) { next(error); } };
export const deleteTransaction = async (req, res, next) => { try { const transaction = await Transaction.findByIdAndDelete(req.params.id); if (!transaction) { res.status(404); throw new Error('Transaction not found'); } res.json({ success: true, message: 'Transaction deleted' }); } catch (error) { next(error); } };
