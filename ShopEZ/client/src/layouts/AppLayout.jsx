import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { useAuth } from '../context/AuthContext.jsx';
export default function AppLayout() { const { user, logout } = useAuth(); const navigate = useNavigate(); const out = () => { logout(); navigate('/'); }; const links = [['/dashboard','Dashboard'],['/market','Market'],['/portfolio','Portfolio'],['/transactions','Transactions'],['/profile','Profile']]; const admin = [['/admin','Admin'],['/admin/users','Users'],['/admin/stocks','Stocks'],['/admin/transactions','All Trades']]; return <div className="app-shell"><aside className="sidebar"><NavLink className="brand" to="/dashboard"><img src="/logo.svg" />ShopEZ</NavLink><nav>{links.map(([to, label]) => <NavLink key={to} to={to}>{label}</NavLink>)}{user?.role === 'admin' && <div className="admin-links">{admin.map(([to, label]) => <NavLink key={to} to={to}>{label}</NavLink>)}</div>}</nav></aside><main className="content"><header className="topbar"><div><span className="eyebrow">Trading workspace</span><h5>{user?.name}</h5></div><button className="btn btn-outline-primary" onClick={out}>Logout</button></header><Outlet /></main></div>; }
